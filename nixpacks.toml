[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:server"]

[start]
cmd = "node server/dist/index.js"

[variables]
NODE_ENV = "production"
